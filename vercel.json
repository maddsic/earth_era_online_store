{
    "env": {
      "NODE_ENV": "production"
    },
    "buildCommand": "pnpm install --legacy-peer-deps && pnpm run build",
    "outputDirectory": "public",
    "installCommand": "pnpm install --legacy-peer-deps"
  }